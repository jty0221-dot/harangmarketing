@AGENTS.md

# 하랑 홈페이지 — 디자인 규칙

이모지 금지·lucide 매핑표 등 전역 규칙은 `E:\하랑\CLAUDE.md` 에 있다. 여기는 홈페이지 전용 기준만 적는다.

## 디자인 기준 — 원티드 디자인 시스템(WDS)

- **하랑 홈페이지(E:\하랑\harang)는 원티드 디자인 시스템(WDS) 기준을 따른다** (2026-08-22 대표 결정)
  · Primary #0066FF · 쿨그레이 뉴트럴 · 토큰은 `app/globals.css`(정본), 컴포넌트는 `app/wds.css`
  · **그라데이션 아이콘 박스 금지** — 단색 배경 + 흰 아이콘으로. 토스풍 장식을 걷어냈다.
  · 재촉형 UI 금지 — '마감 임박' 깜빡임, 가짜 실시간 알림 같은 건 쓰지 않는다.
  · 자세한 규칙: `E:\하랑\harang\docs\design-system.md`

- 로컬루션(#3182F6 토스 스타일 · 그라데이션 아이콘 박스)과 **정반대 기준**이다. 여기서는 그라데이션을 쓰지 않는다.
- 그림자 `shadow-sm` · 라운드 `rounded-2xl`(카드) `rounded-xl`(버튼·입력) `rounded-lg`(작은 박스)

## 반응형 — PC + 모바일 양쪽 가독성

**모든 페이지/컴포넌트는 PC와 모바일 양쪽에서 동시에 가독성·간결성·균형을 만족해야 함.**

#### 컨테이너 너비 제한 (필수)
- **메인 페이지 컨테이너**: `max-w-6xl mx-auto` (1152px) — 사이드바 있는 어드민 페이지
- **단일 카드 폭**: `max-w-4xl` (896px) 이내 — 한 줄에 너무 길게 늘어지면 가독성 떨어짐
- **PC에서 우측 공백 OK** — 끝까지 채우려고 무리하게 늘리지 말 것 (컨텐츠 폭 vs 화면 폭 ≠ 같음)

#### 반응형 그리드 표준 패턴
- **2분할**: `grid-cols-1 md:grid-cols-2 gap-4` (모바일 1열 → 태블릿/PC 2열)
- **QR + 정보**: `grid-cols-1 lg:grid-cols-[300px_1fr] gap-6` (모바일 세로 → PC QR 왼쪽 고정 + 우측 늘어남)
- **3카드**: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`
- **6카드 (배민/네이버 등)**: `grid-cols-2 md:grid-cols-3 lg:grid-cols-6` (모바일 2열 → PC 한 줄)

#### 패딩·간격 (모바일 좁게, PC 넉넉하게)
- 카드 패딩: `p-4 md:p-6` (모바일 16px, PC 24px)
- 섹션 간격: `space-y-4 md:space-y-6`
- 페이지 패딩: `px-4 md:px-6 lg:px-8 py-4 md:py-6`
- 버튼 패딩: `px-3 py-2 md:px-4 md:py-2.5` (PC는 살짝 큼직)

#### 폰트 크기 (모바일 vs PC)
- 페이지 타이틀: `text-xl md:text-2xl font-black`
- 카드 타이틀: `text-base md:text-lg font-bold`
- 본문: `text-sm` (둘 다 동일 OK)
- 보조: `text-xs md:text-[13px]`

#### 긴 텍스트·URL 처리 필수
- **인코딩된 URL은 디코딩 후 표시** (`%EC%9D%BC` → `일`) — `decodeURIComponent` 사용
- **긴 URL/문자열**: `truncate` 또는 `line-clamp-1` 적용 + 툴팁으로 전체 보여주기
- `break-all`은 **최후의 수단** — 한글이 음절 단위로 깨짐
- 모노스페이스 폰트는 코드/짧은 ID에만 (긴 URL은 일반 폰트 + truncate)

#### 모바일 추가 체크리스트
- 터치 타겟 최소 **44×44px** (`py-2.5` 이상)
- 가로 스크롤 절대 금지 — 항상 `overflow-x-hidden` 또는 `min-w-0`
- 텍스트 입력 시 줌 방지 — input 폰트 `text-base` (16px 미만이면 iOS 자동 줌)
- 가로 스와이프 카테고리 탭은 `overflow-x-auto scrollbar-hide` 패턴

#### 새 컴포넌트 만들 때 체크 순서
1. 모바일 (375px) — 컨텐츠가 잘리지 않는가? 가로 스크롤 없는가?
2. 태블릿 (768px) — 1→2열 전환 자연스러운가?
3. 데스크톱 (1280px+) — 한쪽으로 쏠리지 않는가? max-w 적절한가?
4. 큰 모니터 (1920px+) — 컨텐츠가 너무 늘어지지 않는가?

**같은 실수 반복 = 신뢰도 하락. 새 UI 만들 때 항상 위 4단계 체크.**

기존 페이지 수정 시에도 같은 기준 적용 — `max-w-` 없이 컨테이너 만들면 자동 추가.

---

## 화면에 찍히는 한글 문구 — 표기 규칙 (2026-08-29 (토) · D-0107)

**네이버도 인스타도 남의 땅이고 홈페이지만 우리 것이다.** 그래서 여기 적힌 문장은 우리가 한 약속으로 읽힌다.

- **긴 대시(—)를 쓰지 않는다.** 마침표로 끊거나 가운뎃점(`·`)을 쓴다. 한국어 문장에 원래 없던 부호라 AI 표식 1순위다
- 굵게는 별표가 아니라 **`font-semibold` 클래스로 준다.** JSX 안에 `**` 를 적으면 화면에 별표가 그대로 나온다
- **코드 안 주석과 변수명은 검사 대상이 아니다** — 방문자 눈에 들어가는 문자열만 본다
- MDX · JSON 으로 원고를 먼저 쓰는 경우에는 `## 발행 본문` 제목 아래 두고 검사기를 통과시킨 뒤 코드로 옮긴다.
  종료 코드 0 이 통과다

```bash
python E:/하랑/본부장/원고검사.py <파일>
```

- 정본은 `E:\하랑\본부장\원고표기규칙.md` 다. **가격·성과 문구 변경은 그대로 C 급 결재다** (헌장 C-35) — 표기를 고치는 김에 숫자를 같이 바꾸지 않는다
