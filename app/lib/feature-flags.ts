/**
 * 화면 노출 스위치 — 상품을 지우지 않고 감출 때 쓴다.
 *
 * SNS 부스트 스토어(/sns)는 2026-08-31 (월) 대표 지시로 감췄다.
 * 코드·DB·기존 주문·기존 회원은 그대로 두고, 손님 눈에 보이는 길만 막는다.
 * 지운 게 아니라 감춘 것이라 약관·개인정보·환불 조항은 그대로 남긴다.
 *
 * 되살리는 법 (둘 중 하나 · 코드를 되돌릴 필요 없다)
 *  1) Vercel 환경변수에 NEXT_PUBLIC_SNS_STORE=on 을 넣고 재배포한다
 *  2) 아래 기본값을 true 로 바꿔 커밋한다
 *
 * NEXT_PUBLIC_ 접두어라 브라우저 번들에도 들어간다. 비밀값이 아니라 켜고 끄는
 * 스위치라서 노출돼도 문제되지 않는다.
 */
export const SNS_STORE_ENABLED = process.env.NEXT_PUBLIC_SNS_STORE === "on";
